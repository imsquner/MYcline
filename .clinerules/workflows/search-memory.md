# /search-memory — 搜索历史记忆

此工作流用于搜索 `.cline_memory/` 中的历史记录。

## 执行步骤

1. **解析搜索条件**
   - 关键词搜索：在 category_index.json 中匹配
   - 分类搜索：按主类别/子类筛选
   - 时间范围：按日期范围筛选

2. **返回搜索结果**
   - 按相关度排序，返回前 5 条
   - 每条包含：任务名、分类、日期、简述
   - 用户可选择展开查看完整记录
